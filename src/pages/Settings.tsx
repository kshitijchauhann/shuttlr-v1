import { Flex, Text } from "@chakra-ui/react";

const Settings = () => {
    return (
        <Flex p={6}>
            <Text fontSize="xl" fontWeight="bold">
                Settings Content
            </Text>
        </Flex>
    );
};

export default Settings;
